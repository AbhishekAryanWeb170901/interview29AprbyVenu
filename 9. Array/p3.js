//find minimum in arr
var arr=[1,-90,3,67,9,-9,-45];
var pad = -Infinity;
for(var i=0; i<arr.length; i++)
{
    if(arr[i]>pad)
    {
        pad==arr[i];
    }}
    console.log(pad);



    console.log("                                              ");





    var train = [ 25, 30, 28, 40, 48, 42, 50];

    var max_pad = -Infinity;
    
    
    for(var pos=0; pos<train.length; pos++ )
    {
      if(train[pos]>max_pad)
      {
        max_pad = train[pos];
      }
    }
    
    console.log(max_pad);